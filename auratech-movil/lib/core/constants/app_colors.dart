import 'package:flutter/material.dart';

class AuraColors {
  // Background
  static const Color background = Color(0xFFF8FAFC);
  static const Color backgroundAlt = Color(0xFFF1F5F9);

  // Surface
  static const Color surface = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderLight = Color(0xFFF1F5F9);

  // Primary (Blue)
  static const Color primary = Color(0xFF2563EB);
  static const Color primaryLight = Color(0xFFEFF6FF);
  static const Color primaryBorder = Color(0xFFBFDBFE);
  static const Color primaryText = Color(0xFF2563EB);

  // Green (Available / Completed / Verified)
  static const Color greenBg = Color(0xFFECFDF5);
  static const Color greenBorder = Color(0xFFA7F3D0);
  static const Color greenText = Color(0xFF059669);

  // Amber (Pending / Warning / In Review)
  static const Color amberBg = Color(0xFFFFFBEB);
  static const Color amberBorder = Color(0xFFFDE68A);
  static const Color amberText = Color(0xFFD97706);

  // Red (Rejected / Cancelled / Inactive)
  static const Color redBg = Color(0xFFFEF2F2);
  static const Color redBorder = Color(0xFFFECACA);
  static const Color redText = Color(0xFFDC2626);

  // Text
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color textDisabled = Color(0xFFCBD5E1);

  // Shadows
  static Color shadowLight = Colors.black.withOpacity(0.02);
  static Color shadowMedium = Colors.black.withOpacity(0.04);
}
